import allTemplatesData from './allTemplatesData';

const initialState = {
  templates: allTemplatesData
}

export default function(state = initialState, action) {
  switch(action.type) {
    default: 
      return state
  }
}